import React, { FC } from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
   {
    display: flex;
  }
`;

const AirPortSelection: FC = () => {
  return <Wrapper>Lotnikx</Wrapper>;
};

export default AirPortSelection;
